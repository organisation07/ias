name: Team CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print CI message
        run: |
          echo "CI workflow running successfully"
          echo "Triggered by commit: ${{ github.sha }}"
          date
